package com.polymorphism;

public class Keynote {
	/*
	 * Polymorphism:- It is concept of Object oriented programming where 
	 *  poly means many morphism means form. The code that can be written in 
	 *  many form is known polymorphism.
	 * 
	 * It is of two types
	 * 1. Compile time polymorphism or overloading:- In overloading method name is
	 * 		same but parameters are different if parameters are same then their
	 * 		order should be different.
	 *  
	 * 2. Runtime polymorphism or overriding:- changing implementation as per 
	 * 	requirement.
	 * 	In overriding method name and parameters are same.
	 * 		parent child relation is must.
	 * 		ex. toString() method
	 * 		  
	 */
}
